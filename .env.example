# JWT Secret for authentication
JWT_SECRET=infralink-super-secret-key

# Supabase Configuration (Optional - Falls back to SQLite if not provided)
SUPABASE_URL=
SUPABASE_KEY=

# SMTP Configuration for Emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="InfraLink Eventos <noreply@infralink.com.br>"
